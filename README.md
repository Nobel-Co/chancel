# chancel

Provable scope isolation for AI retrieval - firm instructions travel, matter data never crosses the wall, enforced below the model boundary.

Work in progress — see `PRPs/prp-12-scope-isolation.md` for the specification.
